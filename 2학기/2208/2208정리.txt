220804

이상한문자만들기
★ 문자열도 인덱스로 접근 가능하다. 그래서 반복문에도 사용 가능.
★ upper, lower : 대문자, 소문자로 바꾸는 함수.
★ join : 리스트를 문자열로 묶어주는 함수. ' '.join(list) 하면 리스트 요소 사이에 빈칸을 넣어가며 묶음.
★ split : 문자열을 ()안의 문자를 기준으로 쪼개서 리스트를 반환하는 함수.
          그냥 빈칸으로 두는 것과 안에 공백문자를 집어넣는 것은 다른 리스트를 반환한다.




220806

시저암호
★ 파이썬에서 아스키코드를 사용할 때 ord로 문자 -> 아스키 / chr로 아스키 -> 문자
   'A' 이런식으로 써도 아스키코드 대소 비교 같은거에 사용 가능. 굳이 아스키코드 찾아서 할 필요 X.
★ if else 한 줄에 쓰기.



220809

수박수박수
★ 4/2 를 하면 결과가 2.0 float형으로 나옴. 이대로는 for 반복문의 range, 문자열 반복을 위한 곱하기에 사용 불가능.
★ / 나누기     // 몫       % 나머지.
★ 문자열 슬라이싱. a = 'Hello' 에서 a[1:4] 하면 ell, a[1:] 하면 ello, a[:4] 하면 hell.

서울에서김서방
★ 리스트에서 listname.index("onggibot") 하면 onggibot이 listname 리스트에서 몇 번 자리에 있는지 나옴.




220829

곱셈
★ list(map(int, reversed(a))). 문자열 a를 reversed로 뒤집어서, map으로 int형으로 바꿔준 후 리스트에 하나씩 넣기.

R2
★ split은 문자열 쪼개는 용도. 정수는 못쪼갬. 쪼개면 리스트가 됨.

초콜릿쪼개기
   n, m 에 숫자를 한 줄에 입력받으로 할 때 (예를 들면 2 3 이런식으로)
   n, m = input().split() 한 다음 n=int(n), m=int(m) 하지말고,
★ n, m = map(int, input().split()) 으로 한 번에. split으로 쪼개서 리스트만들고, map으로 int로 바꿔서 넣기.

오늘 날짜
★ %를 이용한 문자열 포맷팅. 자릿수까지 정할 수 있음. 문자열 s, 정수 d, 실수 f %2d

화성수학
★ {}를 이용해서도 자릿수 정할 수 있음.
★ if, elif, else 한 줄에 사용 가능
★ 리스트 슬라이싱. a[1:]를 이용해서 첫 번째 원소는 빼고 끝까지 사용.